/** Controls state, mouse position, pressed keys, etc. */
const controls = {
    mouse: {x:0,y:0},
    lastMouse: {x:0,y:0},
    panStart: {x:0,y:0},

    /** Pressed keys */
    pressed: {}
}

export default controls;
